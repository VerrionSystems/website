{
  "name": "v3",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:root": "VITE_SITE_BASE=/ VITE_OUT_DIR=../root-dist VITE_EMPTY_OUT_DIR=true vite build",
    "build:pages": "npm run build:root && node scripts/prepare-pages-artifact.mjs && node scripts/check-pages-artifact.mjs",
    "promote:root": "npm run build:pages && node scripts/promote-root.mjs",
    "preview": "npm run build:pages && VITE_SITE_BASE=/ VITE_OUT_DIR=../root-dist vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "5.0.4",
    "lucide-react": "^1.24.0",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "vite": "^6.4.3"
  }
}
